version: '3.8'

services:
  crawlloom:
    build: .
    container_name: crawlloom
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - NODE_ENV=production
    shm_size: '2gb' # Recommended for concurrent headless browser tabs
